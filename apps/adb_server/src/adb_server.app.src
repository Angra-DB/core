{application, adb_server, [
    {description, "The angra-db TCP server."},
    {vsn, "0.0.1"},
    {modules, []},
    {registered, [adb_server_sup]},
    {applications, [kernel, stdlib, lager]},
    {mod, 
        {adb_server_app, [
            {persistence, adbtree}, %% {persistence, ets | hanoidb | adbtree}
            {distribution, none}      %% {distribution, short | {long, Host::atom()} | none}
        ]}
    }
]}.
